{
  "name": "portal-docs",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --parallel --filter @portal/auth-api --filter @portal/documents-api --filter @portal/documents-web dev",
    "start": "pnpm --parallel --filter @portal/auth-api --filter @portal/documents-api --filter @portal/documents-web start"
  }
}
